---
// Un solo sitio donde se escribe JSON-LD. `is:inline` para que Astro no toque el
// script; el JSON va tal cual lo construye la pagina.
const { datos } = Astro.props;
---
<script type="application/ld+json" is:inline set:html={JSON.stringify(datos)} />
